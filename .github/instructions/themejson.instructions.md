# Instruction: theme.json & Tokens

- Prefer `settings` → `color`, `spacing`, `typography` tokens.
- Consume tokens with `var(--wp--preset--...)` or block supports.
- Avoid inline styles that duplicate tokens.
- Document any required `theme.json` entries in the pattern README.
