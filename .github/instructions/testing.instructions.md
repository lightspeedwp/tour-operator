# Instruction: Testing (PHPUnit, Jest, Playwright)

- Unit test business logic (PHP/JS) with **edge cases** and invalid inputs.
- Playwright flows: insert pattern, edit fields, save, view front-end snapshot.
- Mock WordPress APIs where possible; keep tests deterministic and fast.
- Provide **test IDs/selectors** only where needed; prefer accessible roles/names.
