# Instructions: theme.json & Global Styles

- Define palette/typography/spacing/layout; per-block settings where needed.
- Prefer tokens over bespoke CSS; add style variations in `styles/*.json`.

**Acceptance**
- `"version": 2`; editor presets reflect tokens; JSON validated.
