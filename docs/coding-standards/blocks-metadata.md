# Block Metadata (block.json)

Use **API v3**, fill `title`, `description`, `keywords`, `category`, `icon`.
Add `attributes`, `supports` (color/typography/spacing/border/background), `example`, and `selectors` for fine‑grained style targets.
Register on the server; ensure preview with `example`.
